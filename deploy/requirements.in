# ClaySeal identity backend -- DEPLOYABLE third-party runtime dependency inputs.
#
# This locks the full third-party runtime tree; the package itself has no
# private dependencies (shared-core helpers are vendored in `clayseal/_core.py`).
#
# Compile a hash-pinned lock with either:
#   uv pip compile deploy/requirements.in --generate-hashes \
#       --python-version 3.13 -o deploy/requirements.lock
#   # or, with pip-tools:
#   pip-compile --generate-hashes deploy/requirements.in -o deploy/requirements.lock
#
# Install in the image with:
#   pip install --require-hashes -r deploy/requirements.lock

# --- runtime deps mirrored from clayseal-identity/pyproject.toml ---
httpx>=0.27
biscuit-python>=0.4,<0.5
PyJWT>=2.8,<3.0
cryptography>=42.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
SQLAlchemy>=2.0,<3.0
pydantic>=2.6,<3.0
psycopg[binary]>=3.1,<4.0
alembic>=1.13,<2.0

# --- kms extra mirrored from clayseal-identity/pyproject.toml ---
# boto3 is imported lazily by AwsKmsProvider when
# CLAYSEAL_SECRET_ENCRYPTION_PROVIDER=aws_kms. The production Dockerfile installs
# `.[server,kms]`; this lock input keeps the deploy dependency tree equivalent
# when using hash-pinned installs.
boto3>=1.43.45
